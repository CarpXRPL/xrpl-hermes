# FINAL DEV TEST AUDIT — xrpl-hermes v1.3.4

You are auditing an open-source XRPL developer toolkit. A developer should be able to clone this repo, install dependencies, run any command, and get correct output. Every fact must be accurate. Every command must work. Every example must compile.

## STRUCTURE OF THE AUDIT

### PHASE 1: TOOL VERIFICATION (scripts/xrpl_tools.py)
Test EVERY SINGLE tool command against the live XRPL mainnet. Verify output is correct.

#### Query tools — run each against real addresses:
1. `python3 scripts/xrpl_tools.py account rHb9CJAWyB4rj91VRWn96DkukG4bwdtyTh`
   - Verify: reserve displayed (should be ~1.0 XRP base + ~0.2 XRP per object)
   - Verify: balance shown, flags decoded correctly
2. `python3 scripts/xrpl_tools.py trustlines rD9yBuvcn8cCJf9up4JNZZkn5Zhcb1Zuog`
   - Should show trustlines with currency, issuer, balance, limit
3. `python3 scripts/xrpl_tools.py account_objects rHb9CJAWyB4rj91VRWn96DkukG4bwdtyTh`
   - Should show ledger objects if any
4. `python3 scripts/xrpl_tools.py decode <HEX>` — try with a known tx blob
5. `python3 scripts/xrpl_tools.py tx-info <REAL_TX_HASH>` — fetch a known mainnet tx
6. `python3 scripts/xrpl_tools.py ledger` — should show current ledger index, hash, close time, total XRP
7. `python3 scripts/xrpl_tools.py server-info` — should show version, uptime, state
8. `python3 scripts/xrpl_tools.py nft-info <REAL_NFT_ID>` — fetch real NFT metadata
9. `python3 scripts/xrpl_tools.py book-offers XRP USD:rvYAfWj5gh67oV6fW32ZzP3Aw4Eubs59B` — should show live orderbook
10. `python3 scripts/xrpl_tools.py path-find rHb9CJAWyB4rj91VRWn96DkukG4bwdtyTh rD9yBuvcn8cCJf9up4JNZZkn5Zhcb1Zuog 100 XRP` — path finding
11. `python3 scripts/xrpl_tools.py evm-balance 0x... mainnet` — EVM sidechain
12. `python3 scripts/xrpl_tools.py evm-bridge mainnet` — bridge status
13. `python3 scripts/xrpl_tools.py hooks-info rADDRESS` — should handle missing gracefully
14. `python3 scripts/xrpl_tools.py flare-price XRP BTC ETH` — price feeds
15. `python3 scripts/xrpl_tools.py balance rHb9CJAWyB4rj91VRWn96DkukG4bwdtyTh` — alias for account
16. `python3 scripts/xrpl_tools.py hooks-bitmask HOOK1 HOOK2` — verify it outputs warning about being broken

#### Build tools — each should produce valid signer-ready JSON:
17-45. Every build-* command — verify produces valid XRPL transaction JSON

Check specifically:
- `build-payment --from rSENDER --to rDEST --amount 1000000` → valid Payment JSON
- `build-payment --from rSENDER --to rDEST --amount 100 --cur USD --iss rISSUER` → valid IOU Payment
- `build-trustset --from rADDR --currency USD --issuer rISS --value 1000000000` → valid TrustSet
- `build-offer --from rADDR --taker-pays 1000000 --taker-gets XRP` → valid OfferCreate
- `build-nft-mint --from rADDR --uri ipfs://Qm... --taxon 0` → valid NFTokenMint
- `build-amm-create --from rADDR --amount1 1000000 --amount2 100/USD:rISS --fee 600` → valid AMMCreate
- `build-escrow-create --from rSENDER --to rDEST --amount 1000000 --finish-after 2000000000` → valid EscrowCreate
- `build-check-create --from rSENDER --to rDEST --amount 1000000` → valid CheckCreate
- `build-paychannel-create --from rSENDER --to rDEST --amount 1000000 --settle-delay 86400 --public-key ED...` → valid PaymentChannelCreate
- `build-clawback --from rISSUER --destination rHOLDER --amount 100 --currency USD` → valid Clawback
- `build-mpt-issuance-create --from rADDR --maximum-amount 1000000000000000 --asset-scale 6` → valid MPTokenIssuanceCreate
- `build-mpt-authorize --from rADDR --mpt-issuance-id HEXID --holder rHOLDER` → valid MPTokenAuthorize
- `build-set-oracle --from rADDR --oracle-doc-id 1 --provider 5052494345 --asset-class 554e434c --last-update-time 2000000000` → valid OracleSet
- `build-credential-create --from rADDR --subject rSUBJ --credential-type 4C65616E496E` → valid CredentialCreate
- `build-credential-accept --from rADDR --issuer rISS --credential-type 4C65616E496E` → valid CredentialAccept
- `build-credential-delete --from rADDR --credential-type 4C65616E496E --subject rSUBJ` → valid CredentialDelete
- `build-cross-currency-payment --from rSENDER --to rDEST --deliver 100 --send-max 5000 --currency USD --issuer rISS` → valid cross-currency Payment
- `build-batch --from rADDR --txns '[{"TransactionType":"Payment",...}]'` → valid Batch
- `build-account-delete --from rADDR --to rDEST` → valid AccountDelete
- `build-deposit-preauth --from rADDR --authorize rSENDER` → valid DepositPreauth
- `build-signer-list-set --from rADDR --quorum 2 --signers rA:1,rB:1` → valid SignerListSet
- `build-set-regular-key --from rADDR --regular-key rKEY` → valid SetRegularKey
- `build-amm-deposit --from rADDR --asset1 1000000 --asset2 100/USD:rISS --amount 100` → valid AMMDeposit
- `build-amm-withdraw --from rADDR --asset1 1000000 --asset2 100/USD:rISS --lp-amount 100` → valid AMMWithdraw
- `build-amm-vote --from rADDR --asset1 1000000 --asset2 100/USD:rISS --trading-fee 600` → valid AMMVote
- `build-amm-bid --from rADDR --asset1 1000000 --asset2 100/USD:rISS --bid-min 100` → valid AMMBid
- `build-escrow-finish --from rADDR --owner rOWN --offer-sequence 1` → valid EscrowFinish
- `build-escrow-cancel --from rADDR --owner rOWN --offer-sequence 1` → valid EscrowCancel
- `build-check-cash --from rADDR --check-id HEXID --amount 1000000` → valid CheckCash
- `build-check-cancel --from rADDR --check-id HEXID` → valid CheckCancel
- `build-paychannel-fund --from rADDR --channel-id HEXID --amount 1000000` → valid PaymentChannelFund
- `build-paychannel-claim --from rADDR --channel-id HEXID --amount 1000000 --balance 1000000` → valid PaymentChannelClaim
- `evm-contract --from 0xADDR --bytecode 6080604052...` → valid EVM contract deploy JSON
- `key-gen` — should generate keypair

#### CRITICAL CHECKS for each tool:
- Does it crash? (traceback = 🔴 RELEASE BLOCKER)
- Does the JSON output pass JSON validation? (json.loads on output)
- Are all required fields present per xrpl.org spec?
- Are types correct? (Account = string, Amount = string for XRP in drops, Amount = object for tokens, Sequence = number, Flags = number, Fee = string)
- For Payment: is the XRP amount specified in drops (not whole XRP)?
- For token amounts: is the value a valid decimal string?

### PHASE 2: KNOWLEDGE FILE ACCURACY (knowledge/*.md)

Read EVERY knowledge file and verify against current xrpl.org docs:

#### Transaction Types — verify each matches xrpl.org:
- AccountSet flags, AccountRoot fields
- Payment: Amount format (drops for XRP, {currency, issuer, value} for tokens), DestinationTag type
- TrustSet: LimitAmount format, tfSetFrozen/tfSetNoRipple flags
- OfferCreate: TakerGets/TakerPays (XRP = string of drops, token = {currency, issuer, value})
- NFTokenMint: URI encoding (hex), NFTokenTaxon, TransferFee (0-50000 = 0-50%)
- AMM: Asset1/Asset2 format, TradingFee (0-1000 = 0-1%)
- MPTokenIssuanceCreate: AssetScale, MaximumAmount (string), TransferFee
- EscrowCreate: Destination, Amount, FinishAfter/CancelAfter (ripple epoch seconds)
- CheckCreate: Destination, SendMax, Expiration, InvoiceID
- PaymentChannelCreate: Destination, Amount, SettleDelay, PublicKey
- SignerListSet: SignerEntries format, SignerWeight vs SignerQuorum
- Clawback: Account, Amount, Destination
- DIDSet/DIDDelete: DIDDocument, DIDURI, Data fields

#### XLS Standards — verify numbering:
- XLS-20: NFTs ✅
- XLS-30: AMM ✅
- XLS-33: MPTs ✅
- XLS-38: Cross-Chain Bridge (XChain) ✅
- XLS-39: DID ❓ is this right or XLS-40?
- XLS-40: DID (correct per xrpl.org)
- XLS-47: Price Oracle ✅
- XLS-49: Credentials (DID:1dc5ebdfe66e4...) ❓ or XLS-70?
- XLS-56: Batch ✅
- XLS-70: Credentials (verify this is current)

#### Amendment IDs — verify against xrpl.org current amendments:
Check the AMENDMENT_IDS dict in knowledge/37-xrpl-amendments.md matches xrpl.org current amendment IDs exactly.

#### Reserve values:
- Current XRPL validators voted to lower reserves. Live query: `server_info` → `validated_ledger.reserve_base_xrp` and `reserve_inc_xrp`
- Verify knowledge files reference the CURRENT values (not the old 10/2 values)

#### Token issuance checklist:
- DefaultRipple flag required on issuer account
- Domain set recommended
- TransferRate for fee-on-transfer tokens
- TickSize recommended for DEX precision

#### EVM Sidechain:
- Chain ID: 1440000 (mainnet) — verify this is still correct
- RPC endpoint: rpc.xrplevm.org — verify still active
- Block explorer: evm-sidechain.xrpl.org — verify

#### Clawback rules:
- Requires lsfAllowTrustLineClawback flag on issuer
- Only issuer can clawback
- Pre-existing trust lines are grandfathered
- Partial clawback: CAN claw partial amounts (not just full)

#### RLUSD facts:
- Issuer address: rK7dT4q7cWq8A4EpiCvZjJ7jQ6J3Qp3xJm (or whatever is current)
- USD-pegged stablecoin
- Compliance: travel rule, KYC/AML
- Freeze/Clawback capabilities

#### For each file check:
1. Does it have a "Related Files" section at the bottom? If not, ADD ONE.
2. Are there factual errors? Cross-reference with xrpl.org
3. Are code examples syntactically valid?
4. Are URL references still pointing to working pages?

### PHASE 3: DOCUMENTATION ACCURACY

#### SKILL.md:
- Tool table: 48 entries, each maps to a real command in the dispatcher
- Version: should be 1.3.4
- Activation triggers: make sense?
- Behavior patterns: tool commands referenced actually exist?

#### README.md:
- 59 knowledge files?
- 48 working tools?
- Installation instructions accurate? (pip install -r requirements.txt)
- Git clone URL: https://github.com/CarpXRPL/xrpl-hermes.git ?

#### QUICKSTART.md:
- Every shell command produces valid output matching the examples?
- git clone URL matches?
- Reserve values in sample output match current (1.0 base / 0.2 per object)?

#### STANDALONE.md:
- Every example command actually works?
- Script paths correct (scripts/xrpl_tools.py)?
- Reserve values current?
- EVM addresses use 0x prefix?

#### CHANGELOG.md:
- No copy-paste duplicates
- Versions match actual tags

#### SECURITY.md:
- Accurate for the project?
- Contact method valid?

#### LIMITATIONS.md:
- Honest about what doesn't work? (hooks-bitmask, etc.)

#### .env.example:
- Placeholder values, no real secrets?

### PHASE 4: CROSS-FILE CONSISTENCY

- SKILL.md tool table → dispatcher in scripts/xrpl_tools.py: are they identical?
- Knowledge file "Related Files" sections: do they reference existing files?
- Examples/ directory: do examples match the tool API?
- README tool table → SKILL.md tool table: same info?
- STANDALONE.md examples → actual tool command signatures: do they match?

### PHASE 5: ARCHITECTURE & SAFETY

- No hardcoded wallet seeds or private keys
- `env var` references use `os.environ["VAR"]` pattern, not hardcoded values
- No secrets committed to git
- Lazy client init — module loads instantly offline
- Error handling: network failures don't crash with traceback
- All JSON output is valid json

## DELIVERABLE

For every issue found, output in this format:

```
[CLASS] file:line — description — EXACT FIX
```

Where CLASS is:
- **BLOCKER**: Developer hits a crash on documented first use
- **FACTUAL**: Wrong spec, amendment ID, reserve value, protocol detail
- **DOC**: Wrong command, wrong output, stale URL
- **GAP**: Missing "Related Files", missing cross-ref, missing feature
- **CLEANUP**: Dead code, copy-paste duplication, formatting

Then for each BLOCKER and FACTUAL issue, FIX IT IMMEDIATELY using write_file or patch.

After all fixes, run:
```bash
python3 scripts/xrpl_tools.py ledger
```
to confirm the script still loads and runs.

Finally output a COMPLETE VERIFICATION TABLE:

| Phase | What Was Tested | Issues Found | Issues Fixed | Status |
|-------|----------------|--------------|--------------|--------|
| 1 | 48 tool commands | N | N | PASS/FAIL |
| 2 | 59 knowledge files | N | N | PASS/FAIL |
| 3 | Documentation | N | N | PASS/FAIL |
| 4 | Cross-file consistency | N | N | PASS/FAIL |
| 5 | Security/architecture | N | N | PASS/FAIL |

## IMPORTANT NOTES
- You are using Claude Opus 4.7 — be thorough and accurate
- Always verify against live xrpl.org docs if unsure
- Run commands to verify they work — don't just assume
- If you find a broken tool, fix the code AND update the SKILL.md table
- Cross-reference files at the bottom of each knowledge file are REQUIRED for all 59 files
- If you hit turn limits, stop and output partial results with "TURN_LIMIT" marker
